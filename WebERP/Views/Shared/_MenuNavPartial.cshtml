@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager


@if (SignInManager.IsSignedIn(User))
{
    <hr class="sidebar-divider my-0">
    
    <!-- Divider -->
    <hr class="sidebar-divider">
    <!-- Heading -->
    @*<div class="sidebar-heading">
            Admin
        </div>*@


    @*@if (User.IsInRole("Dashboard") || User.IsInRole("Admin"))
    {*@
        <li class="nav-item active">
            <a class="nav-link" asp-controller="Dashboard" asp-action="Dashboard">
                <i class="fas fa-fw fa-tachometer-alt"></i>
                <span>Dashboard</span>
            </a>
        </li>
    //}
    <!-- Divider -->
    <hr class="sidebar-divider">
    <!-- Heading -->
    @*<div class="sidebar-heading">
            Admin
        </div>*@

    <!-- Nav Item - Pages Collapse Menu -->
    @if (User.IsInRole("JobWork") || User.IsInRole("Admin") || User.IsInRole("Employee") || User.IsInRole("Advance") || User.IsInRole("Attandance"))
    {
        <li class="nav-item collapse-item" >
            <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#Payments"
               aria-expanded="true" aria-controls="Payments">
                <i class="fas fa-fw fa-cog"></i>
                <span>Pay Roll</span>
            </a>
            <div id="Payments" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
                <div class="bg-white py-2 collapse-inner rounded">

                    @if (User.IsInRole("Employee") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Employee" asp-action="Employee_Master">Employee Master</a>
                    }
                    @if (User.IsInRole("Advance") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="EmpAdvance" asp-action="Emp_Adv_Details">Employee Advance</a>
                    }
                    @if (User.IsInRole("Attandance") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="EmpAttandance" asp-action="Emp_Attand_Details">Employee Attandance</a>
                    }
                    @if (User.IsInRole("Attandance") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Salary" asp-action="Emp_Sal_Detail">Salary Generation</a>
                    }                    
                </div>
            </div>
        </li>
    }
    @if (User.IsInRole("Payments") || User.IsInRole("Admin") || User.IsInRole("GateEntryUser") || User.IsInRole("PO") || User.IsInRole("GoDownStock") || User.IsInRole("Cutting") || User.IsInRole("RawMaterial") || User.IsInRole("CuttingReceipt") || User.IsInRole("MFG"))
    {
        <li class="nav-item">
            <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#Transactions"
               aria-expanded="true" aria-controls="Transactions">
                <i class="fas fa-fw fa-cog"></i>
                <span>Transactions</span>
            </a>
            <div id="Transactions" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
                <div class="bg-white py-2 collapse-inner rounded">
                    @if (User.IsInRole("ArticalMerge") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="ArticalMerge" asp-action="Artical_Merge_Detail">Artical Merge</a>
                    }
                    @if (User.IsInRole("Ledger") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Ledger" asp-action="Ledger">Ledger Details</a>
                    }
                    @if (User.IsInRole("StockDetails") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="StockDetails" asp-action="StockDetails">Stock Details</a>
                    }
                    @if (User.IsInRole("Payments") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Payments" asp-action="Payments_Details">Payments</a>
                    }
                    @if (User.IsInRole("GateEntryUser") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="GateEntry" asp-action="Gate_Entry_Details">Gate Entry Inward</a>
                    }
                    @if (User.IsInRole("PO") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="PO" asp-action="POGridDetails">Purchase Order</a>
                    }
                    @if (User.IsInRole("GoDownStock") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="GoDown" asp-action="GoDownStock">GoDown Allocation</a>
                    }
                    @if (User.IsInRole("Cutting") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Cutting" asp-action="CuttingDetail">Cutting Order / Job Card</a>
                    }
                    @if (User.IsInRole("RawMaterial") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="RawMaterial" asp-action="RM_Detail">Raw Material Issue</a>
                    }
                    @if (User.IsInRole("CuttingReceipt") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="CuttingReceipt" asp-action="CuttingReceiptDetail">Cutting Receipt</a>
                    }
                    @if (User.IsInRole("MFG") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="MFGReceipt" asp-action="MFGReceiptDetail">Process Receipt</a>
                    }
                    @if (User.IsInRole("JobWork") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="JobWork" asp-action="JobWorkGrid">Job Work Issue</a>
                    }
                    @if (User.IsInRole("JobWork") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Sales" asp-action="SalesGrid">Sale Invoice</a>
                    }
                </div>
            </div>
        </li>
    }
    @if (User.IsInRole("Location") || User.IsInRole("Admin") || User.IsInRole("Company") || User.IsInRole("Account") || User.IsInRole("Term") || User.IsInRole("UOM") || User.IsInRole("Brand") || User.IsInRole("Size") || User.IsInRole("Artical") || User.IsInRole("Item") || User.IsInRole("ProcessRate") || User.IsInRole("Process") || User.IsInRole("Department") || User.IsInRole("GoDown"))
    {
        <li class="nav-item">
            <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseUser"
               aria-expanded="true" aria-controls="collapseUser">
                <i class="fas fa-fw fa-cog"></i>
                <span>Masters</span>
            </a>
            <div id="collapseUser" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
                <div class="bg-white py-2 collapse-inner rounded">
                    @*<h6 class="collapse-header">Users:</h6>*@

                    @if (User.IsInRole("Tax") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Tax" asp-action="Tax_Detail">Tax</a>
                    }
                    @if (User.IsInRole("AgentCommRate") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="AgentCommRate" asp-action="AgentCommRateDetails">Agent Commission Rate</a>
                    }
                    @if (User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" href="/Identity/Account/register">Register User</a>
                    }
                    @if (User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Administration" asp-action="ListRole">Roles</a>
                    }
                    @if (User.IsInRole("Location") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Location" asp-action="LocationDetails">Locations Details</a>
                    }
                    @*@if (User.IsInRole("Company") || User.IsInRole("Admin"))
        {
            <a class="collapse-item" asp-area="" asp-controller="Company" asp-action="Company">Company Master</a>
        }*@
                    @if (User.IsInRole("Account") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-area="" asp-controller="Account" asp-action="Account_Master">Account Master</a>
                    }
                    @if (User.IsInRole("Term") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Term" asp-action="Term_Master">Term Master</a>
                    }
                    @if (User.IsInRole("UOM") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="UOM" asp-action="UOM_Master">UOM Master</a>
                    }
                    @if (User.IsInRole("Brand") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Brand" asp-action="Brand_Master">Brand Master</a>
                    }
                    @if (User.IsInRole("Size") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Size" asp-action="Size_Master">Size Master</a>
                    }
                    @if (User.IsInRole("Artical") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Artical" asp-action="Artical_Master">Artical Master</a>
                    }
                    @if (User.IsInRole("Item") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Item" asp-action="Item_Master">Item Master</a>
                    }
                    @if (User.IsInRole("Process") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Process" asp-action="Process_Master">Process Master</a>
                    }
                    @if (User.IsInRole("ProcessRate") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="ProcessRate" asp-action="ProcessRate_Master">Artical Process Rate</a>
                    }
                    @if (User.IsInRole("GoDown") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="GoDown" asp-action="GoDown_Master">GoDown Master</a>
                    }
                    @if (User.IsInRole("Department") || User.IsInRole("Admin"))
                    {
                        <a class="collapse-item" asp-controller="Department" asp-action="Department_Master">Department Master</a>
                    }
                    @*<a class="collapse-item" asp-area="" asp-page="/Privacy">User Profile</a>*@

                </div>
            </div>
        </li>
    }
        <!-- Nav Item - Utilities Collapse Menu -->
        @*<li class="nav-item">
                <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseUtilities"
                   aria-expanded="true" aria-controls="collapseUtilities">
                    <i class="fas fa-fw fa-wrench"></i>
                    <span>Expenses</span>
                </a>
                <div id="collapseUtilities" class="collapse" aria-labelledby="headingUtilities"
                     data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <h6 class="collapse-header">Utilities:</h6>
                        <a class="collapse-item" href="#">Expense Category</a>
                        <a class="collapse-item" href="#">Expense Detail</a>
                    </div>
                </div>
            </li>

            <!-- Nav Item - Pages Collapse Menu -->
            <li class="nav-item">
                <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseEmp"
                   aria-expanded="true" aria-controls="collapseEmp">
                    <i class="fas fa-fw fa-cog"></i>
                    <span>Employee</span>
                </a>
                <div id="collapseEmp" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <h6 class="collapse-header">Employee Controls:</h6>
                        <a class="collapse-item" asp-area="" asp-page="/Index">Add Employee</a>
                        <a class="collapse-item" asp-area="" asp-page="/Privacy">Employee Salary</a>
                        <a class="collapse-item" asp-area="" asp-page="/Index">Extra Time Details</a>
                        <a class="collapse-item" asp-area="" asp-page="/Privacy">Attendance</a>
                    </div>
                </div>
            </li>
            <!-- Divider -->
            <hr class="sidebar-divider">
            <!-- Heading -->
            <div class="sidebar-heading">
                Masters
            </div>*@
        <!-- Nav Item - Pages Collapse Menu -->
        @*<li class="nav-item">
                <a class="nav-link" href="#" data-toggle="collapse" data-target="#collapseStocks" aria-expanded="true"
                   aria-controls="collapseStocks">
                    <i class="fas fa-fw fa-folder"></i>
                    <span>Stock</span>
                </a>
                <div id="collapseStocks" class="collapse" aria-labelledby="headingPages"
                     data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <h6 class="collapse-header">Manage Stocks</h6>
                        <a class="collapse-item" href="login.html">Products</a>
                        <a class="collapse-item" href="register.html">Super Product</a>
                        <a class="collapse-item" href="forgot-password.html">Purchase</a>
                        <a class="collapse-item" href="forgot-password.html">Current Stock</a>
                        @*<div class="collapse-divider"></div>
                            <h6 class="collapse-header">Other Pages:</h6>
                            <a class="collapse-item" href="404.html">404 Page</a>
                            <a class="collapse-item active" href="blank.html">Blank Page</a>
                    </div>
                </div>
            </li>*@
        <!-- Divider -->
        <hr class="sidebar-divider">
        @*<li class="nav-item">
                <a class="nav-link" href="charts.html">
                    <i class="fas fa-fw fa-chart-area"></i>
                    <span>Add Cash</span>
                </a>
            </li>*@
        <!-- Nav Item - Charts -->
        @*<li class="nav-item">
                <a class="nav-link" href="charts.html">
                    <i class="fas fa-fw fa-chart-area"></i>
                    <span>Reports</span>
                </a>
            </li>*@
        <!-- Nav Item - Tables -->
        @*<li class="nav-item">
                <a class="nav-link" href="tables.html">
                    <i class="fas fa-fw fa-table"></i>
                    <span>Settings</span>
                </a>
            </li>*@
    }


